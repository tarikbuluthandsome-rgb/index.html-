{
  "version": 2,
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
```

**Kaydet** (Ctrl+S) → kapat → sonra:
```
git add .
git commit -m "vercel duzeltildi"
git push